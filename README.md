# BookingAppReactNodeMongo

`cd Backend`
 `npm  npinstall`

 `npm start`

Go to Frontend Repository
`cd Frontend`
 `npm install` 
 `npm start`

Note: I know I pushed .env file, which should not be here... Just for simplification.
